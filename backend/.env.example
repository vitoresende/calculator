# Port for Go REST API server (Cloud Run provides this automatically via $PORT)
PORT=8080

# Allowed origins for CORS (set to your frontend domain in production, or * for local dev)
ALLOWED_ORIGINS=*
