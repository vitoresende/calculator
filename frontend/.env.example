# Base URL for the Go REST API.
# In local development with Vite proxy, use /api/v1 or http://localhost:8080/api/v1
# In production on Firebase Hosting, set to your Google Cloud Run service URL:
# https://calculator-backend-<hash>-<region>.a.run.app/api/v1
VITE_API_BASE_URL=/api/v1
